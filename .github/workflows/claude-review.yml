name: Claude PR Review Disabled

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Claude PR review is disabled. Use PR Review by OpenHands."
